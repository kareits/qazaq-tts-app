# URL backend API для сборки frontend.
# Локальная разработка (backend на 8000):
VITE_BACKEND_URL=http://127.0.0.1:8000
# Прод за одним доменом (frontend и API на одном origin) — оставить пустым,
# тогда используются относительные пути /api/... :
# VITE_BACKEND_URL=
