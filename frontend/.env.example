# Backend API URL used at frontend build time.
# Local development (backend on 8000):
VITE_BACKEND_URL=http://127.0.0.1:8000
# Production behind a single domain (frontend and API on one origin) — leave
# empty, then relative /api/... paths are used:
# VITE_BACKEND_URL=
