# iSoyle — план запуска в прод (isoyle.kz)

Приложение уже контейнеризовано (один домен: Caddy отдаёт фронтенд и проксирует
`/api`, авто-HTTPS), есть rate-limit и мониторинг. Ниже — пошаговый чеклист от
покупки домена до публичного запуска.

Порядок: сначала поднять VPS (нужен его IP для DNS) → затем настроить DNS домена
→ деплой → HTTPS выпустится автоматически.

---

## Текущий статус (обновляется по ходу)

**🎉 РАБОТАЕТ В ПРОДЕ: https://isoyle.kz** (валидный сертификат Let's Encrypt,
HTTP→HTTPS редирект, www тоже). Сервер поднят, защищён, синтез проверен
end-to-end по HTTPS. Осталось: юридические документы + эксплуатационные хвосты.

- ✅ Домен `isoyle.kz` куплен (ps.kz), DNS `A → 2.27.131.208` (+ CNAME www) — Фаза 1.
- ✅ VPS (megahost, Ubuntu 24.04, 4 vCPU / 3.8 ГБ RAM / swap 4 ГБ) — Фаза 2.
- ✅ Базовая защита (пользователь `isoyle`, ufw, fail2ban, unattended-upgrades,
      sshd: root-логин и пароли выключены, только ключ) — Фаза 3.
- ✅ Docker Engine + compose — Фаза 4.
- ✅ Деплой (сборка на сервере, 3 голоса female3/male1/male2, `docker compose up`)
      — Фаза 5.
- ✅ HTTPS: сертификат на `isoyle.kz` + `www.isoyle.kz`, синтез по HTTPS ok — Фаза 6.
- ✅ Эксплуатация: лог-ротация, rate-limit, restart-политика, CD-скрипт,
      uptime-мониторинг (UptimeRobot) — Фаза 7.
- ⏸️ Юридическое (privacy/ToS/ISSAI/товарный знак) — Фаза 8, **отложено**
      (сделать до публичного/коммерческого анонса).
- 📱 Мобильное приложение — вынесено в отдельный план [MOBILE.md](MOBILE.md).

**Запуск инфраструктуры завершён.** Сайт живёт на https://isoyle.kz; остаётся
юридическая часть (Фаза 8, к моменту анонса) и мобильное приложение (см. отдельный
план).

---

## Фаза 0. Решения перед стартом
- [x] **VPS-провайдер** — megahost.kz.
- [ ] Сделать репозиторий **приватным** (для коммерции; репо пока публичный —
      деплой идёт клоном по HTTPS).
- [x] Способ деплоя: **сборка образов на сервере** (Вариант A).

## Фаза 1. Домен isoyle.kz ✅
- [x] Куплен на **ps.kz** (NS: ns1/ns2/ns3.ps.kz).
- [x] DNS-записи:
  - `A  isoyle.kz  → 2.27.131.208` ✅
  - `CNAME www → isoyle.kz` ✅
  - (записи `mail`/`MX`/`SPF` от ps.kz — почта, к сайту не относятся)
- [x] DNS распространился (Google 8.8.8.8 и Cloudflare 1.1.1.1 отдают `2.27.131.208`).

## Фаза 2. VPS ✅
- **Развёрнутая конфигурация**: megahost VPS, **Ubuntu 24.04.4 LTS**, 4 vCPU,
  3.8 ГБ RAM, 69 ГБ SSD. IP `2.27.131.208`, hostname `srv.isoyle.kz`.
- [x] **Swap увеличен 512 МБ → 4 ГБ**, `vm.swappiness=10` (RAM 3.8 ГБ впритык под
      torch + 3 голоса в кэше; swap закрывает OOM-риск).
- [x] Вход по SSH-ключу (`id_isoyle`, отдельный от GitHub-ключа).
- ⚠️ **Подводный камень CPU**: изначально гипервизор выдавал модель
      `QEMU Virtual CPU 2.5+` (qemu64) без SSE4.2/POPCNT (x86-64-v2) → NumPy 2.x /
      torch падали при импорте, backend не собирался. Провайдер по заявке сменил
      модель на **Intel Broadwell** — после этого всё заработало без правок кода.
      Проверка: `grep -w sse4_2 /proc/cpuinfo`.

## Фаза 3. Базовая защита сервера ✅
- [x] Пользователь `isoyle` с `sudo` (+ группа `docker`, NOPASSWD sudo, вход по
      ключу). Восстановление — через VNC-консоль провайдера.
- [x] sshd: `PermitRootLogin no`, `PasswordAuthentication no`,
      `KbdInteractiveAuthentication no` — только ключ (проверено: root/пароль сразу
      отклоняются `Permission denied (publickey)`).
      ⚠️ **Подводный камень**: Ubuntu держит `/etc/ssh/sshd_config.d/50-cloud-init.conf`
      с `PasswordAuthentication yes`, а sshd берёт **первое** значение по алфавиту,
      поэтому `99-hardening.conf` (`no`) молча перебивался и пароль оставался
      включён. Исправлено: `no` выставлено во **всех** источниках (основной
      `sshd_config`, `50-cloud-init.conf`, `99-hardening.conf`). Проверять эффективное
      значение: `sudo sshd -T | grep -i passwordauthentication`.
- [x] Firewall `ufw`: разрешены `22`, `80`, `443`; остальное deny.
- [x] `unattended-upgrades` (автообновления безопасности) + `fail2ban`
      (jail sshd: 5 попыток → бан 1 ч). ⚠️ Домашний IP админа занесён в `ignoreip`
      (jail.local), иначе случайная попытка входа под root (он отключён) быстро
      приводит к самоблокировке. Если всё же забанило — зайти с другого IP
      (мобильный хотспот/VPN) → `fail2ban-client set sshd unbanip <ip>`, либо VNC.
- [x] hostname `srv.isoyle.kz`.

## Фаза 4. Docker ✅
- [x] Docker Engine 29.6 + compose plugin v5.3 (офиц. apt-репозиторий).
- [x] Пользователь `isoyle` в группе `docker`.

## Фаза 5. Деплой iSoyle ✅ (Вариант A — сборка на сервере)
- [x] `git clone` публичного репозитория в `/opt/isoyle`.
- [x] `cp .env.example .env` (пока `SITE_ADDRESS=:80`, HTTP на IP).
- [x] Модели в volume `isoyle_models` — **продовый набор из 3 голосов**
      (female3, male1, male2): `docker compose run --rm backend python
      scripts/download_kazakhtts2.py female3 male1 male2`.
      ⚠️ Скрипт раньше писал в `/app/models` (хардкод), игнорируя `MODELS_DIR`;
      **исправлено** — теперь уважает `MODELS_DIR`, и модели идут в смонтированный
      том. (Правку закоммитить + запушить.)
- [x] `docker compose build` → `docker compose up -d`. Backend `healthy`.

**Вариант B — готовые образы из GHCR (быстрее на сервере):**
- [ ] Создать тег релиза локально: `git tag v0.1.0 && git push origin v0.1.0` → CI
      соберёт и запушит `ghcr.io/kareits/qazaq-tts-app-{backend,web}`.
- [ ] На сервере: `docker login ghcr.io`, отдельный `docker-compose.prod.yml` с
      `image:` вместо `build:`, `docker compose pull && up -d`. (Могу подготовить
      этот файл.)

**HTTPS ✅:** после DNS выставили `SITE_ADDRESS="isoyle.kz, www.isoyle.kz"` в
`.env` → Caddy выпустил сертификаты Let's Encrypt на оба имени, HTTP→HTTPS
редирект автоматом.
- ⚠️ **Пробел после запятой обязателен**: Caddy требует `isoyle.kz, www.isoyle.kz`
  (с пробелом); без пробела — ошибка «Site addresses cannot contain a comma».
- ⚠️ **Сертификаты Caddy надо persistиить**: в `docker-compose.yml` добавлены
  volume `caddy_data:/data` и `caddy_config:/config`, иначе при пересоздании
  контейнера сертификаты перевыпрашиваются и можно упереться в лимит LE
  (5 сертификатов/домен в неделю). На сервере продублировано в
  `docker-compose.override.yml` (untracked), пока правка репо не запушена.
- Редирект `www → apex` — пока оба имени отдают сайт одинаково; отдельный
  редирект можно добавить позже (полировка).

## Фаза 6. Проверка
- [x] `/api/health` ok; синтез (female3/male1) → валидный mp3 (22.05 kHz, 128 kbps).
- [x] Сайт открывается снаружи; backend:8000 наружу не торчит.
- [x] `https://isoyle.kz` открывается, сертификат валиден (TLS verify OK);
      `http://` → 308 на `https://`; `www.isoyle.kz` тоже валиден.
- [x] Синтез проверен через боевой HTTPS-домен (200 → mp3).
- [x] Rate-limit срабатывает на проде: 10× `400` → `429` с кастомным сообщением;
      реальный IP из `X-Forwarded-For` за Caddy.
- [x] Логи: `docker compose logs -f` (ротация настроена, см. Фазу 7).

## Фаза 7. Эксплуатация
- [x] **Лог-ротация Docker** — `/etc/docker/daemon.json` (`max-size 10m`,
      `max-file 3`), контейнеры пересозданы, применено к обоим.
- [x] **`restart: unless-stopped`** — подтверждено на backend и web (переживут
      перезагрузку сервера).
- [x] **Бэкапы**: критичного состояния нет — модели воспроизводимы скриптом,
      `storage/cache` одноразовый; достаточно сохранить `.env`. (Пароль
      восстановления `isoyle` — в менеджере паролей.)
- [ ] **Rate-limit** под реальную нагрузку — при необходимости поднять
      `TTS_RATE_LIMIT` в `.env` (сейчас `10/minute`, проверено — работает).
- [x] **Uptime-мониторинг**: UptimeRobot, keyword-монитор на
      `https://isoyle.kz/api/health` — инцидент, когда пропадает `"model_loaded":true`
      (ловит и падение сервера, и отвал модели), интервал 5 мин, алерт на email.
- [ ] (опц.) **Prometheus + Grafana** на сервере, скрейп `backend:8000/metrics`
      (метрики внутренние, наружу не торчат).
- [x] **CD-скрипт** `scripts/deploy.sh` — одна команда на сервере:
      `cd /opt/isoyle && ./scripts/deploy.sh` (или с dev-машины
      `ssh isoyle 'cd /opt/isoyle && ./scripts/deploy.sh'`). Делает `git pull`
      (с ре-exec самого скрипта), пересборку, `up -d`, ожидание healthcheck и
      smoke-тест `/api/health`; при провале — логи + команда отката.
- [ ] (опц.) **CD через GHCR**: по тегу `v*` собираются образы →
      `docker compose pull && up -d` (быстрее, чем сборка на сервере). При росте
      нагрузки можно перейти на этот вариант.

### Настройка UptimeRobot (5 минут, бесплатный тариф)
1. Регистрация на uptimerobot.com.
2. **Add New Monitor** → тип **HTTP(s)**.
3. URL: `https://isoyle.kz/api/health`, интервал **5 мин**.
4. (опц.) **Keyword monitor** вместо HTTP: ключевое слово `"status":"ok"` —
      поймает случай, когда сервер отвечает 200, но модель отвалилась.
5. Alert Contacts: email (и/или Telegram) — уведомление при падении.
   `/api/health` отвечает мгновенно даже во время синтеза, так что ложных
   срабатываний из-за нагрузки не будет.

## Фаза 8. Юридическое (до публичного/коммерческого запуска) — ⏸️ ОТЛОЖЕНО
> Пропущено на текущем этапе по решению. Обязательно вернуться до публичного
> анонса и до публикации в сторах (Google Play Data Safety требует privacy policy).
- [ ] **Политика конфиденциальности** — раскрыть, что текст отправляется на сервер
      для синтеза (страница `isoyle.kz/privacy`). Обязательна и для будущего Google Play.
- [ ] **Условия использования (ToS)** — включить этический пункт KazakhTTS2
      (не генерировать оскорбительный/дискриминационный контент).
- [ ] **ISSAI** — письмо (issai@nu.edu.kz) с подтверждением коммерческого
      использования и согласованием формулировки атрибуции.
- [ ] **Товарный знак** «iSoyle» — проверка/регистрация в Qazpatent.
- Атрибуция уже в UI (футер Credits & Licenses) ✓.

## Фаза 9. После запуска
- [ ] (опц.) **Cloudflare** перед доменом: бесплатный CDN/защита от DDoS. Нюанс:
      при проксировании Cloudflare Caddy не сможет пройти HTTP-01 — на старте
      держать DNS-only (серое облако), Caddy сам выпустит сертификат; проксирование
      включить позже с DNS-01 или origin-сертификатом.
- [ ] **Мобильное приложение** (Capacitor) поверх живого API `isoyle.kz` —
      вынесено в отдельный план [MOBILE.md](MOBILE.md).
- [ ] **Масштабирование** (A-5): Redis (общий rate-limit между воркерами) + очередь
      Celery/RQ при росте нагрузки.

---

## Критический путь на день запуска — ✅ ПРОЙДЕН
1. ✅ ~~Поднять VPS + базовая защита + Docker~~ (Фазы 2–4).
2. ✅ ~~Деплой + модели~~ (Фаза 5).
3. ✅ ~~Купить домен~~ (Фаза 1).
4. ✅ ~~Прописать DNS `A isoyle.kz → 2.27.131.208`~~ (Фаза 1).
5. ✅ ~~`SITE_ADDRESS` → домен → Caddy выпустил HTTPS → `https://isoyle.kz`~~ (Фаза 6).

**iSoyle доступен на https://isoyle.kz.** Фазы 1–7 завершены. Осталось:
- **Фаза 8 (юридическое)** — отложена, обязательна до публичного/коммерческого анонса.
- **Мобильное приложение** — отдельный план [MOBILE.md](MOBILE.md).

Инфраструктурный запуск закрыт.
